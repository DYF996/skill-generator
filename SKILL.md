---
name: skill-generator
description: 自然语言命令：直接执行/做、先计划/计划、生成技能/创建技能、引导我/咨询/帮我分析。自动判断意图，可生成、执行或规划任务。支持智能咨询引导模式，通过提问挖掘真实需求并辅助创作解决方案。
---

# Skill Generator

> ⚠️ **重要说明**：当前 AI 无法自动保存文件到你的电脑，也无法跨对话记忆。本技能生成的 Skill 需要你**手动复制内容并保存**到本地（例如 `~/.claude/skills/` 或自定义目录）。新对话时，请将 Skill 内容粘贴给 AI 或描述任务名称即可复用。

## 自然语言命令
- `直接执行 需求` 或 `做 需求`：直接执行，不输出计划
- `先计划 需求` 或 `计划 需求`：先输出步骤清单，确认后执行
- `生成技能 需求` 或 `创建技能 需求`：生成完整 Skill（单文件文本块，可一键复制）
- `引导我 问题` 或 `咨询 问题` 或 `帮我分析 问题`：进入智能咨询模式，AI 通过提问引导你理清问题并给出解决方案

## 自动判断（无命令时）
- 含"做/帮我/执行/处理" → 直接执行模式
- 含"步骤/计划/怎么" → 先计划模式
- 含"生成/创建 Skill/技能" → 生成技能模式
- **需求模糊时**（描述笼统、缺少关键信息、用户表达困惑/迷茫）→ 自动进入智能咨询模式

## 简化模式
- 用户输入"简化模式"后，AI 用自然语言总结步骤（不展示技术细节）
- 输入"详细模式"或"显示步骤"恢复技术清单
- 模式状态在对话中记忆，每次回复前检查
- **注意**：生成技能模式和智能咨询模式始终使用详细模式（不受简化/详细模式影响）

## 执行规则
### 先计划模式
1. 输出步骤清单（每步一行，简要说明）
2. 询问："确认执行？(回复"确认"或提出修改)"
3. 用户确认后逐步执行，关键节点给出进度
4. 某步失败则停止并提示："第N步失败，请手动处理后重新执行"
5. 失败后询问："是否重试？(是/否) 或 '修改需求' 重新开始"

### 直接执行模式
- 直接执行，失败则停止并提示，然后询问是否重试或修改需求

### 生成技能模式（适配当前场景）
当用户要求生成 Skill 或同意生成后，AI 应输出**单个完整文本块**，包含：

1. **Skill 基本信息**（名称、描述）
2. **完整 SKILL.md 内容**（YAML frontmatter + 正文）
3. **可选的脚本/参考内容**（如果任务需要脚本，以代码块形式附在同一个消息中）
4. **保存和使用的说明**（告诉用户如何保存到本地、如何在下次对话中使用）

**输出格式示例**（用户只需复制整个代码块）：

```markdown
--- 复制以下内容到文件：my_skill/SKILL.md ---
---
name: my-skill
description: 描述...
---
# 标题
## 适用场景
...
## 执行步骤
...
## 约束条件
...
## 示例
...
--- 可选：如果需要脚本，另存为 scripts/run.py ---
```python
# 脚本内容
```

--- 使用说明 ---

1. 创建文件夹 my_skill/
2. 保存 SKILL.md 和脚本
3. 新对话中说："使用 my-skill 技能 处理 XXX"

```

---

## 🧠 智能咨询模式（V2.0 新增）

### 触发方式
- 用户明确说：`引导我 [问题]` / `咨询 [问题]` / `帮我分析 [问题]`
- 自动触发：当 AI 检测到用户问题**模糊不清、缺少关键信息、或用户表现出困惑/迷茫时**（描述过于简略、没有具体目标和约束、使用了"不知道怎么办"等表达）

### 核心理念
> 很多时候，用户并不是没有答案，而是**没有把问题说清楚**。AI 的角色不是猜，而是**通过结构化提问，帮用户自己理清思路**。

### 工作流程（五阶段）

#### 阶段一：初步理解 & 确认边界
1. AI 用自己的话**简要复述**当前听到的问题（1-2句话，确认理解是否正确）
2. 如果不准确，用户立即纠正；如果准确，进入下一阶段

**输出格式：**
```
🧭 我先确认一下我听到的问题：
> [用1-2句话总结当前理解]

这是你的意思吗？如果不是，请帮我纠正。
```

#### 阶段二：结构化提问（3-5个问题）
AI 从以下维度筛选**最关键的3-5个问题**逐一提问（不要一口气全问，一次问一个，等用户回答后再问下一个）：

| 维度 | 提问目的 | 示例 |
|------|----------|------|
| 🎯 **目标澄清** | 你要达到什么具体结果？ | "你希望最终得到什么？一个文件、一个自动化流程，还是别的？" |
| 🔍 **现状摸底** | 现在是什么情况？ | "目前这个问题你是怎么处理的？遇到了什么障碍？" |
| ⛓️ **约束条件** | 有什么限制？ | "有哪些不能动或者必须遵守的东西？比如时间、预算、技术栈？" |
| 👤 **使用场景** | 给谁用、怎么用？ | "这个解决方案是谁在用？用一次还是反复用？" |
| 📏 **成功标准** | 什么叫"做好了"？ | "你怎么判断问题已经解决了？有没有具体指标？" |
| 🔧 **已有工具** | 手头有什么可用的？ | "你现在用的是什么工具/平台？有没有现成的账号或API？" |
| ⏱️ **紧急程度** | 多急？ | "有时间限制吗？是立即可取的还是可以慢慢打磨？" |

**输出格式（一次只问一个）：**
```
💡 第 1/4 个问题：
[具体问题]
```

#### 阶段三：综合诊断 & 方案建议
收集完所有回答后，AI 输出综合分析：

```
📊 问题分析报告
─────────────────
🎯 核心问题：[一句话概括真实需求]

📋 关键发现：
- [发现1]
- [发现2]
- [发现3]

🔑 根因分析：
[为什么会出现这个问题？根本原因是什么？]

─────────────────
💡 可行方案（按推荐度排序）

方案A：[名称]
- 思路：[一句话]
- 优点：[2-3条]
- 缺点/风险：[1-2条]
- 适用条件：[什么情况下选这个]

方案B：[名称]
- 思路：[一句话]
- 优点：[2-3条]
- 缺点/风险：[1-2条]
- 适用条件：[什么情况下选这个]

方案C：（如有）
...

─────────────────
👉 我的推荐：方案[X]，理由：[1-2句话]

你想选哪个方案？或者有什么想法？
```

#### 阶段四：用户选择 & 执行
- 用户选定方案后，AI 判断：
  - 如果是**一次性任务** → 进入直接执行模式或先计划模式
  - 如果是**可复用的流程** → 建议生成 Skill
- 执行过程中，AI 可以**同步创造辅助工具**（如生成脚本、模板、配置文件）

#### 阶段五：验证 & 固化
- 执行完毕后，询问是否满足预期
- 如果需要调整，回到优化流程
- 如果满意，检测是否为重复性任务并建议生成 Skill

### 智能咨询模式 - 完整示例

**用户**：`帮我分析 我的店铺数据太乱了，不知道怎么整理`

**AI**：
> 🧭 我先确认一下我听到的问题：
> > 你有一些店铺相关的数据，但目前比较混乱，你不知道从哪里下手整理。
> 
> 这是你的意思吗？如果不是，请帮我纠正。

**用户**：`对，就是这意思`

**AI**：
> 💡 第 1/5 个问题：
> 这些数据具体是什么类型？销售数据、库存、客户信息，还是多个类型混在一起？

**用户**：`主要是每天的销售数据和客户购买记录，在好几个Excel文件里`

**AI**：
> 💡 第 2/5 个问题：
> 你希望整理完之后达到什么效果？是想做数据分析出报表，还是只想把它们合并到一个文件方便查找？

**用户**：`我想每个月能自动生成一份销售分析报告，看看哪些产品卖得好`

**AI**：
> 💡 第 3/5 个问题：
> 你现在用的平台或工具有哪些？数据是从哪里导出的？

**用户**：`数据从淘宝后台导出的Excel，我用的电脑是Windows系统`

**AI**：
> 💡 第 4/5 个问题：
> 这份月报需要给谁看？是你自己用还是要发给老板/团队？

**用户**：`我自己看就行，主要是想快速了解经营状况`

**AI**：
> 💡 第 5/5 个问题：
> 你对技术操作熟悉吗？比如能不能接受用Python脚本自动处理，还是希望完全不需要写代码的方案？

**用户**：`不太会代码，最好简单一点`

**AI**：
> 📊 问题分析报告
> ─────────────────
> 🎯 核心问题：需要每月自动从淘宝导出的Excel中生成销售分析报告
> 
> 📋 关键发现：
> - 数据源：淘宝后台导出的Excel（销售+客户购买记录）
> - 目标：月报，分析产品销售排行
> - 用户：自己看，不需对外
> - 约束：Windows系统、不会代码、希望简单
> 
> 🔑 根因分析：
> 问题不是"数据乱"，而是"缺乏自动分析和可视化流程"。原始数据有规律，但你缺少一个标准化的处理管线。
> 
> ─────────────────
> 💡 可行方案
> 
> 方案A：AI对话式处理（推荐）
> - 思路：生成一个"销售月报"Skill，每月把Excel拖进来，一句话让AI帮你分析出报告
> - 优点：零门槛、不需要装任何软件、快速上手
> - 缺点：每次需要手动发起对话
> - 适用条件：数据量不大（每次<1000行）
> 
> 方案B：WPS/Excel 模板 + 公式
> - 思路：创建一个预设好公式和图表的Excel模板，每月粘贴数据进去自动出结果
> - 优点：离线可用、所见即所得
> - 缺点：需要花时间搭建模板、模板改动麻烦
> - 适用条件：报表格式固定不变
> 
> ─────────────────
> 👉 我的推荐：方案A，因为最简单、零代码、立即可用，且你可以先验证流程再看要不要升级。
> 
> 你想选哪个方案？或者有什么想法？

**用户**：`就方案A吧`

**AI**：
> 好的！我现在帮你生成"销售月报"Skill，完成之后我们试用一下。

（进入生成技能模式，输出完整 SKILL.md 文本块...）

（生成完成后）
> ✅ Skill 已生成！是否在当前对话中试用一下？把你的Excel数据发给我，或者简单描述几条数据，我模拟跑一次。

**用户**：`试用`

**AI**：
> 模拟执行销售月报分析：
> - 读取数据 → 3个工作表（每日销售×30、客户购买×15、退货×2）
> - 清洗合并 → 有效销售记录 487 条
> - 分析排行：
>   🥇 产品A - ¥12,340 (占比 28%)
>   🥈 产品B - ¥8,920 (占比 20%)
>   🥉 产品C - ¥5,610 (占比 13%)
> - 月总销售额：¥44,000
> 
> 试用成功！请保存 Skill 以便下月直接使用。

---

## 重复性任务检测与 Skill 生成建议

在每次任务执行完成后（直接执行模式或先计划模式确认执行后），AI 应自动分析本次任务是否可能为重复性任务。

### 判断依据（满足任一即触发询问）
- 用户输入含明确周期词：每天/每周/每月/每年/每日/周报/日报/月报/例行/定时
- 用户输入含固定路径模式：`*.csv`、`~/data/`、`/logs/`、`report_*.xlsx`
- 用户输入含"经常"、"每次"、"总是"、"重复"、"以后还要做"等词
- 用户明确要求"以后还要做"

**不再使用"步骤数≥3"条件**，以减少误判。

### 询问模板

---
🔁 检测到该任务可能具有重复性（原因：<具体原因>）。
是否需要为这个任务创建一个 Skill？下次您只需一句话就能自动完成，可显著降低 token 消耗（通常 50%–80%）。
回复"是"立即生成 Skill，回复"否"忽略。
---

### 防止过度询问
- 同一任务特征（输入的前 50 个字符 + 主要操作）在同一对话中只询问一次。
- 用户连续拒绝 2 次后，本次对话不再主动询问任何任务的 Skill 生成。
- 用户发送"不再询问生成 Skill"后，本对话内永久关闭主动询问。
- 用户使用 `生成技能` 命令时，不触发此询问。
- 用户明确在任务描述中说"不要生成 Skill"时，跳过询问。

### 询问后的处理
- 用户回复"是"：自动进入 `生成技能` 流程，基于本次任务的具体步骤生成 Skill 文本块。
- 用户回复"否"：记录拒绝次数，继续对话。

## 增强功能：立即试用刚生成的 Skill
在生成 Skill 后，AI 应主动询问：
> 是否在当前对话中试用这个 Skill？您只需提供一组测试输入（或使用刚才的任务数据），我会模拟执行 Skill 中的步骤，验证效果。
- 用户回复"是"：AI 使用生成的 SKILL.md 中的"执行步骤"模拟运行一次（不要求真实文件操作）。
- 用户回复"否"：结束流程，提供保存提示。

## 跨对话使用指南（每次生成 Skill 后附带）

📁 如何保存并使用这个 Skill：

1. 创建文件夹，例如 my_skill/
2. 将上面的 SKILL.md 内容保存到该文件夹（文件名必须为 SKILL.md）
3. 如果有脚本，保存到 scripts/ 子文件夹
4. 在新对话中，告诉 AI："请加载 ~/my_skill/ 中的 Skill" 或者直接说"使用 my-skill 技能做 XXX"
 （注意：AI 不会自动加载，你需要手动粘贴 SKILL.md 内容到新对话，或使用平台支持的 Skill 加载机制）

---

## 适用场景
- 用户想自动执行一次性或重复性任务，并希望 AI 自动检测重复性、建议生成 Skill。
- 用户需要将一组操作封装成可复用的 Skill，并能手动保存和跨对话使用。
- **【新增】用户有模糊需求或困惑，需要AI通过提问帮助理清思路、挖掘真实问题、诊断根因并给出方案。**
- **【新增】用户需要AI辅助创作工具、脚本或模板来解决问题，而不只是给建议。**

## 执行步骤
1. 解析用户输入，判断模式（直接执行/先计划/生成技能/智能咨询）。
2. 根据模式执行任务：
   - 咨询模式：五阶段流程（确认→提问→诊断→选择→验证固化）
   - 其他模式：按原有规则执行
3. 任务完成后，依据判断条件分析是否为重复性任务，若是则询问是否生成 Skill。
4. 根据用户反馈进入生成技能流程，输出可复制的文本块。
5. 询问是否立即试用生成的 Skill（可选）。
6. 提供保存和跨对话使用说明。

## 约束条件
- AI 无法自动保存文件，需要用户手动复制和保存。
- AI 无法跨对话记忆 Skill，新对话需要用户重新提供 Skill 内容或使用平台支持的 Skill 加载机制。
- 生成的脚本可能需要用户根据实际环境调整（路径、权限、依赖等）。
- 重复性检测依赖关键词，可能漏判或误判。
- **智能咨询模式的关键原则：一次只问一个问题，等待回答后再继续；不预判用户答案；所有方案必须可执行。**

## 示例

### 示例1：生成技能（原有功能）

**用户**：`每天下午5点把 /logs/error.log 复制到 ~/backup/`

**AI**（直接执行模式）：
> 已模拟执行复制操作（实际环境请确保路径存在）。
> 
> 🔁 检测到该任务可能具有重复性（原因：含周期词"每天"+固定路径 `/logs/`）。
> 是否需要为这个任务创建一个 Skill？下次您只需一句话就能自动完成。
> 回复"是"立即生成 Skill，回复"否"忽略。

**用户**：`是`

**AI**：
> 请为该 Skill 命名（如不指定则自动命名为 `daily-error-log-backup`）：

**用户**：`log-backup`

**AI**：
> 已生成 Skill，请复制以下内容保存：
> 
> ```markdown
> --- 复制以下内容到文件：log-backup/SKILL.md ---
> ---
> name: log-backup
> description: 每天下午5点将 /logs/error.log 复制到 ~/backup/
> ---
> 
> # 日志备份技能
> 
> ## 适用场景
> 需要定期备份 error.log 文件
> 
> ## 执行步骤
> 1. 检查源文件 /logs/error.log 是否存在
> 2. 检查目标目录 ~/backup/ 是否存在，不存在则创建
> 3. 复制文件：cp /logs/error.log ~/backup/error_$(date +%Y%m%d).log
> 4. 输出备份完成信息
> 
> ## 约束条件
> - 需要读取 /logs/ 目录的权限
> - 目标目录需要有写入权限
> 
> ## 示例
> 
> 用户：执行 log-backup
> AI：已备份 error.log 到 ~/backup/error_20260410.log
> ```
> 
> --- 使用说明 ---
> 1. 创建文件夹 log-backup/
> 2. 保存上述内容为 SKILL.md
> 3. 新对话中说："使用 log-backup 技能备份日志"
> ```
> 
> 是否在当前对话中试用这个 Skill？（回复"是"我将模拟执行一次）

**用户**：是

**AI**：
> 模拟执行：
> - 假设当前时间 17:00
> - 检查 /logs/error.log → 存在
> - 创建 ~/backup/ → 已存在
> - 复制并重命名为 error_20260410.log
> - 输出：✅ 备份完成
> 
> 试用成功。请按上述说明保存，以便下次使用。

### 示例2：智能咨询模式（新增功能）
详见上方"智能咨询模式 - 完整示例"章节。